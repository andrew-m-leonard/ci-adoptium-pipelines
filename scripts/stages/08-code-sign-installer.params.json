{
  "stageId": "08-code-sign-installer",
  "stageDisabled": false,
  "stageCondition": [
    { "param": "ENABLE_INSTALLERS", "value": true },
    { "param": "SIGN_ARTIFACTS",    "value": true }
  ],
  "description": "Gate-only params.json for the code-sign-installer stage. Runs only when both ENABLE_INSTALLERS and SIGN_ARTIFACTS are true.",
  "parameterGroups": [
    {
      "name": "Stage Selections",
      "description": "Boolean flags that control which pipeline stages are enabled. All stage-gate parameters are collected here so operators can see and adjust the full execution plan in one place.",
      "parameters": [
        {
          "name": "SIGN_ARTIFACTS",
          "type": "boolean",
          "default": false,
          "description": "Enable artifact signing."
        }
      ]
    }
  ]
}
