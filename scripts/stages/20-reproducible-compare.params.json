{
  "stageId": "20-reproducible-compare",
  "stageDisabled": false,
  "stageCondition": [
    { "param": "RUN_REPRODUCIBLE_COMPARE", "value": true },
    { "param": "SCM_REF", "value": "regex:.+" }
  ],
  "description": "Parameters consumed by the reproducible build comparison stage. Vendor overrides may suppress, replace, or extend these via a vendor-scripts/20-reproducible-compare.params.json in the config repo.",
  "parameterGroups": [
    {
      "name": "Stage Selections",
      "description": "Boolean flags that control which pipeline stages are enabled. All stage-gate parameters are collected here so operators can see and adjust the full execution plan in one place.",
      "parameters": [
        {
          "name": "RUN_REPRODUCIBLE_COMPARE",
          "type": "boolean",
          "default": false,
          "description": "Run the reproducible build comparison stage. Requires SCM_REF to be set. Downloads the corresponding production Adoptium binary and diffs it against the locally produced build. Disabled by default."
        }
      ]
    }
  ]
}
