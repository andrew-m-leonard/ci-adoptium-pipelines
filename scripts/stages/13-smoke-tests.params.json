{
  "stageId": "13-smoke-tests",
  "stageDisabled": false,
  "stageCondition": [
    { "param": "RUN_TESTS", "value": true }
  ],
  "description": "Gate-only params.json for the smoke-tests stage. Runs only when RUN_TESTS is true.",
  "parameterGroups": [
    {
      "name": "Stage Selections",
      "description": "Boolean flags that control which pipeline stages are enabled. All stage-gate parameters are collected here so operators can see and adjust the full execution plan in one place.",
      "parameters": [
        {
          "name": "RUN_TESTS",
          "type": "boolean",
          "default": true,
          "description": "Enable test stages."
        }
      ]
    }
  ]
}
